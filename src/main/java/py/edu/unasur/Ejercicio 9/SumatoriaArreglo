import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
public class SumArrayController {

    @PostMapping("/api/algorithms/sum-array")
    public int sumArray(@RequestBody List<Integer> numbers) {
        int sum = 0;
        for (int number : numbers) {
            sum += number;
        }
        return sum;
    }
}